// สต็อกขั้นต่ำเป็น 0 = อะไหล่ที่ซื้อจากร้านอะไหล่มาใช้เลย ไม่ได้เก็บไว้ในร้าน
// เหลือ 0 จึงเป็นสภาวะปกติของมัน ไม่ใช่ของหมด — ไม่ต้องเตือน และต้องเบิกลงบิลซ่อมได้ตามปกติ
export const tracksStock = (minStockLevel) => Number(minStockLevel) > 0;
