// คืนค่าว่างเมื่อยังไม่รู้บทบาท ไม่ใช่ "พนักงาน" — ไม่งั้นแอดมินจะเห็นบทบาทผิดแวบหนึ่งระหว่างรอโหลด
export const roleLabel = (role) => {
  if (role === "ADMIN") return "แอดมิน";
  return role ? "พนักงาน" : "";
};
